# AgentX 一体化镜像环境配置文件
# 复制此文件为 .env 并修改相应配置

# ===========================================
# 🚀 服务端口配置
# ===========================================
SERVER_PORT=8088

# ===========================================
# 🗄️ 数据库配置（内置PostgreSQL）
# ===========================================
# 使用内置数据库时的默认配置
DB_HOST=localhost
DB_PORT=5432
DB_NAME=agentx
DB_USER=agentx_user
DB_PASSWORD=agentx_pass

# 如需使用外部数据库，请设置以下变量并在启动时添加 -e EXTERNAL_DB_HOST=your-db-host
# EXTERNAL_DB_HOST=your-external-postgres-host
# DB_HOST=your-external-postgres-host
# DB_PORT=5432
# DB_NAME=agentx
# DB_USER=your-db-user
# DB_PASSWORD=your-db-password

# ===========================================
# 🐰 消息队列配置（内置RabbitMQ）
# ===========================================
# 使用内置RabbitMQ时的默认配置
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USERNAME=guest
RABBITMQ_PASSWORD=guest

# 如需使用外部RabbitMQ，请设置以下变量并在启动时添加 -e EXTERNAL_RABBITMQ_HOST=your-mq-host
# EXTERNAL_RABBITMQ_HOST=your-external-rabbitmq-host
# RABBITMQ_HOST=your-external-rabbitmq-host
# RABBITMQ_PORT=5672
# RABBITMQ_USERNAME=your-mq-user
# RABBITMQ_PASSWORD=your-mq-password

# ===========================================
# 👤 系统用户配置
# ===========================================
# 管理员账户
AGENTX_ADMIN_EMAIL=admin@agentx.ai
AGENTX_ADMIN_PASSWORD=admin123
AGENTX_ADMIN_NICKNAME=AgentX管理员

# 测试用户（生产环境建议禁用）
AGENTX_TEST_ENABLED=true
AGENTX_TEST_EMAIL=test@agentx.ai
AGENTX_TEST_PASSWORD=test123
AGENTX_TEST_NICKNAME=测试用户

# ===========================================
# 🔐 安全配置
# ===========================================
# JWT密钥（生产环境请更改）
JWT_SECRET=Qm9MTHM4R0hHcXlKQ0N5M2N1V1I1b0JqN1Fqa1pVQ0N2eVQ=

# ===========================================
# 📧 邮件服务配置（可选）
# ===========================================
MAIL_SMTP_HOST=smtp.qq.com
MAIL_SMTP_PORT=587
MAIL_SMTP_USERNAME=
MAIL_SMTP_PASSWORD=
MAIL_VERIFICATION_TEMPLATE=您的验证码是:%s，有效期10分钟，请勿泄露给他人。
MAIL_VERIFICATION_SUBJECT=AgentX - 邮箱验证码

# ===========================================
# 🔗 OAuth配置（可选）
# ===========================================

# GitHub集成配置
GITHUB_REPO_NAME=agent-mcp-community
GITHUB_USERNAME=lucky-aeon
GITHUB_TOKEN=your_github_token

# ===========================================
# 🏗️ 高可用网关配置（可选）
# ===========================================
HIGH_AVAILABILITY_ENABLED=false
HIGH_AVAILABILITY_GATEWAY_URL=http://localhost:8081
HIGH_AVAILABILITY_API_KEY=default-api-key-1234567890

# ===========================================
# 🧠 向量数据库配置
# ===========================================
# 默认使用主数据库配置，如需单独配置：
VECTOR_DB_HOST=localhost
VECTOR_DB_PORT=5432
VECTOR_DB_USER=agentx_user
VECTOR_DB_PASSWORD=agentx_pass
VECTOR_DB_NAME=agentx
VECTOR_DB_TABLE=public.vector_store
VECTOR_DB_DIMENSION=1024
VECTOR_DB_DROP_TABLE_FIRST=false
VECTOR_DB_CREATE_TABLE=true

# ===========================================
# 🤖 AI服务配置（可选）
# ===========================================
# SiliconFlow重排服务
SILICONFLOW_API_KEY=
SILICONFLOW_API_URL_RERANK=
SILICONFLOW_MODEL_RERANK_MODEL=Pro/BAAI/bge-reranker-v2-m3
SILICONFLOW_TIMEOUT=30000


# ===========================================
# ☁️ 对象存储配置（可选）
# ===========================================

# AWS S3配置
S3_SECRET_ID=
S3_SECRET_KEY=
S3_REGION=
S3_ENDPOINT=
S3_BUCKET_NAME=
S3_DOMAIN=

# ===========================================
# 💳 支付配置（可选）
# ===========================================
# 支付宝配置
ALIPAY_APP_ID=your_alipay_app_id
ALIPAY_PRIVATE_KEY=your_alipay_private_key
ALIPAY_PUBLIC_KEY=your_alipay_public_key

# Stripe配置
STRIPE_SECRET_KEY=your_stripe_secret_key
STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key
STRIPE_WEBHOOK_SECRET=your_stripe_webhook_secret

# ===========================================
# 📝 日志配置
# ===========================================
LOG_LEVEL_ROOT=info
LOG_LEVEL_APP=debug

# ===========================================
# 📁 文件存储配置
# ===========================================
FILE_STORAGE_PATH=/app/storage

# ===========================================
# 🐳 容器管理配置
# ===========================================
# Docker连接配置（生产环境必须配置）
AGENTX_CONTAINER_DOCKER_HOST=unix:///var/run/docker.sock
# 用户数据卷基础路径
AGENTX_CONTAINER_USER_VOLUME_PATH=/docker/users
# 默认MCP网关镜像
AGENTX_CONTAINER_DEFAULT_MCP_IMAGE=ghcr.io/lucky-aeon/mcp-gateway:latest
# 后端容器访问宿主机地址（Docker部署建议: host.docker.internal）
AGENTX_CONTAINER_HOST_ACCESS_ADDRESS=host.docker.internal
# MCP网关部署超时配置（毫秒）
MCP_GATEWAY_CONNECT_TIMEOUT=60000
MCP_GATEWAY_READ_TIMEOUT=180000
# MCP SSE长连接超时（毫秒）
CHAT_TOOLS_MCP_TRANSPORT_TIMEOUT_MS=300000
# 容器监控间隔（毫秒）
AGENTX_CONTAINER_MONITOR_INTERVAL=300000
# 资源统计更新间隔（毫秒）
AGENTX_CONTAINER_STATS_INTERVAL=120000
